<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Log In</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha2/dist/css/bootstrap.min.css" rel="stylesheet"
    integrity="sha384-aFq/bzH65dt+w6FI2ooMVUpc+21e0SRygnTpmBvdBgSdnuTN7QbdgL+OapgHtvPp" crossorigin="anonymous">

    <style>
      .form-signin {
    max-width: 330px;
    padding: 1rem;
    }  
    </style>
</head>
<body>

  <%- include('_navout') %>

    <main class="form-signin w-100 m-auto">
        <form form id="loginForm" action="/login" method="post">
          <!-- <img class="mb-4" src="../public/img/emoji-smile.svg" alt="" width="72" height="57"> -->
          <h1 class="h3 mb-3 fw-normal">Please log in</h1>

          <!-- Error message area for validation errors and custom error message -->
          <% if (errors && errors.length > 0) { %>
            <div class="alert alert-danger" role="alert">
                <ul>
                    <% errors.forEach(error => { %>
                        <li><%= error.msg %></li>
                    <% }) %>
                </ul>
            </div>
          <% } else if (message) { %>
            <div class="alert alert-danger" role="alert">
                <span id="message"><%= message %></span>
            </div>
          <% } %>
          
          <div class="form-floating mb-3">
            <input type="email" class="form-control" id="email_address" name="email_address" placeholder="name@example.com">
            <label for="email_address">Email addresss <span id="message" ></span></label>
          </div>
          <div class="form-floating mb-3">
            <input type="password" class="form-control" id="password" name="password" placeholder="Password">
            <label for="password">Password <span id="message"></span></label>
          </div>
            <br>
         
          <button class="btn btn-primary w-100 py-2" type="submit">Log in</button>
          
        </form>

        <div>
          <p class="mt-5 mb-3 text-body-secondary">Don't have an account yet?</p>  
            <a href="/register" class="icon-link">
            Register Here
            <img src="/img/chevron-right.svg" alt="chevron-right">
        </a>
        </div>

        <div>
          <p class="mt-5 mb-3 text-body-secondary">Want to update your password?</p>  
            <a href="/changepassword" class="icon-link">
            Change Password
            <img src="/img/chevron-right.svg" alt="chevron-right">
        
      </div>
    </main> 
</body>
</html>